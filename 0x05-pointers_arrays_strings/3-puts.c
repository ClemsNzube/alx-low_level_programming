#include "main.h"

/**
 * _puts - print a string followed by a new line
 * @str: pointer holding the string variable
 */

void _puts(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		_puts(str[i];
	}
	_puts('\n');
}
